import ecs100.*;
/**
 * Class to handle the GUi functionality.
 *
 * @author 13DTC
 * @version 27/5/21
 *
 *    public class GUI
 *    {   
 *        /**
 *         * Constructor for objects class GUI
 *         * 
 *         *
 *        public GUI() {
 *            // instance variables - replace the example below with your own
*            Books bk = new Books();
*            UI.initialise();
*            UI.addButton("All", bk::printAll);
*            UI.addTextField("Name of book:", bk::findBookName);
 *           //UI.addTextField("Id of book:", bk::findBookId);
*            UI.addButton("Quit", UI::quit);
*        }
*}
*/